# Task PRD-0102: Settings And Split Manifest

> PRD: PRD-01 | Depends: PRD-0101
> Files: 3 | Lines: ~150 | Time: ~60min

## What to Build
Add typed settings and canonical train/val/test scene manifests based on appendix D.2.

## Files

### Create: `src/anima_def_ghostfwl/settings.py`
```python
class GhostFWLSettings(BaseSettings):
    dataset_root: Path
    pretrain_root: Path
    checkpoint_root: Path
    input_shape: tuple[int, int, int] = (128, 128, 256)
```

### Create: `src/anima_def_ghostfwl/data/splits.py`
```python
TRAIN_SCENES = ("001", "003", "004", "005", "006", "008", "010")
TEST_SCENES = ("002", "007", "009")
```

## Verify
```bash
uv run pytest tests/test_settings.py tests/test_splits.py -v
```

## Done When
- [ ] split policy is encoded in code and TOML
- [ ] path defaults match project volumes
