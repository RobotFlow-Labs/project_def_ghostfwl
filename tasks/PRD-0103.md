# Task PRD-0103: Dataset IO And Preprocessing

> PRD: PRD-01 | Depends: PRD-0102
> Files: 3 | Lines: ~240 | Time: ~90min

## What to Build
Implement discovery of `.b2` and `.npy` files plus the paper preprocessing path that maps raw `(400,512,700)` volumes to model tensors.

## Files

### Create: `src/anima_def_ghostfwl/data/io.py`
```python
def load_voxel(path: Path) -> np.ndarray:
    ...
```

### Create: `src/anima_def_ghostfwl/data/preprocess.py`
```python
def preprocess_voxel(voxel: np.ndarray) -> torch.Tensor:
    ...
```

## Verify
```bash
uv run pytest tests/test_data_io.py tests/test_preprocess.py -v
```

## Done When
- [ ] crop/downsample path matches paper constants
- [ ] tensor layout is `[1, 256, 128, 128]`
